# The plugin's engine is generated by `bun run build:plugin`, never edited by
# hand. Collapse it in review and keep it out of diffs; CI verifies it matches
# its source on every PR into main. On a merge conflict, rebuild — never
# hand-merge.
plugins/chowa/dist/cli.js linguist-generated=true -diff
